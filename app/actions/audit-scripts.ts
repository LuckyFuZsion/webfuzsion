"use server"

// This file is no longer needed, but we'll keep an empty version
// to avoid import errors in case it's referenced elsewhere
export async function fetchAuditData() {
  return {
    tags: [],
    recommendations: [],
  }
}
